# Optional External Validation Data

This directory is reserved for auxiliary external validation assets.

The main UAV-track assessment protocol and formal manuscript experiment suites do
not require files in this directory. If you need to run
`scripts/run_external_ground_validation.py`, place the optional
`ground_target_glass2.xlsx` file here locally. The spreadsheet is intentionally
not tracked in the clean code repository.
